part of 'controller.dart';

class SampleView extends StatelessView<SampleScreen, SampleController> {
  const SampleView(SampleController state, {Key? key}) : super(state, key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
